// Shared with both the GM's empty-state quick-add buttons
// (CampaignNotes.jsx) and the AI generator's prompt context
// (promptContexts.js), so the priority section list only lives in one
// place.
export const DEFAULT_CAMPAIGN_NOTE_SECTION_NAMES = [
  "Items",
  "Monster Types",
  "Locations",
  "Win Scenarios",
];
